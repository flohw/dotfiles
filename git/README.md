# Git

Git configuration. It relies on `$XDG_CONFIG_HOME` variable as most of the configuration.

## Improvements

- [ ] The `config` file MUST be configurable as `user.email` and `user.name` to prevent email stealing.
- [ ] The `ignore` file may contains more patterns to ignore ?

