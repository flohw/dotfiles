# i3blocks

The whole i3blocks configuration is in its default file as this is not possible to customize it.
The `blocks` directory contains all necessary scripts to display blocks preperly.

## Improvements

- [ ] Make the volume script relies bash script only. It currently uses a bit of perl.

